import { EmBreve } from "@/components/layout/EmBreve";

export default function BuscarPage() {
  return (
    <EmBreve
      title="Pesquisar"
      description="Em breve: busca global por obras, atividades, RDOs, fotos e pessoas. Vamos indexar tudo no início desta semana."
      emoji="🔍"
    />
  );
}
