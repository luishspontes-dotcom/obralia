import { EmBreve } from "@/components/layout/EmBreve";

export default function ConfiguracoesPage() {
  return (
    <EmBreve
      title="Configurações"
      description="Em breve: gestão de usuários, papéis, integrações, branding do tenant, faturamento. Por enquanto, mude senha pelo painel do Supabase ou peça pra mim."
      emoji="⚙️"
    />
  );
}
